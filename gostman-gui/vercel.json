{
    "framework": "vite",
    "buildCommand": "cd frontend && npm install && npm run build:web",
    "outputDirectory": "frontend/dist-web",
    "rewrites": [
        {
            "source": "/api/(.*)",
            "destination": "/api/$1"
        }
    ]
}